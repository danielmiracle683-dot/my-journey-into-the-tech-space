function checkPassword(password) {
    if (password.length >= 8) {
        return "strong password";
    }  else {
        return "password too short";
    }
}
console.log(checkPassword("Danny"));
console.log(checkPassword("Danny@2005"));